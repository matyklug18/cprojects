{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug Bar",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceFolder}/build/bar",
            "args": [""],
            "stopAtEntry": false,
            "cwd": "${workspaceFolder}",
            "environment": [],
            "externalConsole": false,
            "MIMode": "gdb"
        }
    ]
}
